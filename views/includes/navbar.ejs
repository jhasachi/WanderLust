<div class="nav-fixed">
  <nav class="navbar navbar-expand-md k-nav">
    <div class="container-fluid" style="max-width: var(--page-width);">
      <a class="navbar-brand" href="/listings">
        <span class="brand-mark"><i class="fa-regular fa-compass"></i></span>
        <span class="d-none d-sm-inline fw-bold">WanderLust</span>
      </a>
      <button
        class="navbar-toggler"
        type="button"
        data-bs-toggle="collapse"
        data-bs-target="#navbarNavAltMarkup"
        aria-controls="navbarNavAltMarkup"
        aria-expanded="false"
        aria-label="Toggle navigation"
      >
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav ms-auto align-items-center gap-2 mt-2 mt-md-0">
          <a class="pill-btn accent text-decoration-none" href="/listings/new"
            >Add yours</a
          >
          <% if (!currUser) { %>
          <a class="pill-btn text-decoration-none" href="/signup">Sign up</a>
          <a class="pill-btn text-decoration-none" href="/login">Log in</a>
          <% } %> <% if (currUser) { %>
          <a class="pill-btn text-decoration-none" href="/logout">Logout</a>
          <% } %>
        </div>
      </div>
    </div>
  </nav>
</div>
